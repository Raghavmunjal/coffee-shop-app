import { StyleSheet } from "react-native";
import { primaryBlackHex } from "../../constants/colors";

const styles = StyleSheet.create({
    container:{
        flex:1,
        backgroundColor:primaryBlackHex,
    },
    scrollViewContainer:{
        flexGrow:1
    }
});

export default styles;